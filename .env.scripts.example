# Copy to .env.scripts (gitignored). Read only by the Admin SDK scripts
# (pnpm migrate:v3, pnpm provision:user), never by the app.
# Credentials for the project owner, kept apart from the machine's default ADC:
GOOGLE_APPLICATION_CREDENTIALS=/Users/you/.config/gcloud-media-taxonomy/application_default_credentials.json
